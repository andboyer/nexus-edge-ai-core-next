[toolchain]
# TODO(M1): repin to a specific minor version once the workspace builds
# clean once on Linux. Tried 1.82 → needs 1.85 (edition 2024 transitives);
# tried 1.85 → needs 1.88 (image@0.25.10). Using `stable` as an interim
# unblock so dependency-rustc-floor games stop blocking M0 verification.
channel = "stable"
components = ["rustfmt", "clippy"]
profile = "minimal"
